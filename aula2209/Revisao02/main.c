#include <stdio.h>

int main(void) {
    int x, y;
    char l;
    double valor;
    float valor2;
    scanf("%d%d %c", &x, &y, &l);
    printf("Valro x: %d e y:  %d  letra: %c\n", x, y, l);

    return 0;
}