#include <stdio.h>

int main(void) {
    int a;
    char palavra[52] = "Maromo";
    int b, c, d, e;
    int x[100];
    for (int i = 0; i < 100; ++i) {
        printf("Valor de x na posica %d = %d \n", i, x[i]);
    }

    //printf("Valor de a: %d \n", d);
    return 0;
}