#include <stdio.h>

int main(void) {
    char nome[51];
    printf("Digite seu nome: ");
    gets(nome);

    printf("Seu nome e: %s\n", nome);
    for (int i=0; i<51; i++){
      putchar(nome[i]);
    }

    return 0;
}