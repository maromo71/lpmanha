#include <stdio.h>

int main(void) {
    int x = 100;
    double y = 34.56;
    printf("Tamanho de x na memoria: %d bytes\n", sizeof(x));
    printf("Tamanho de y na memoria: %d bytes\n", sizeof(y));
    return 0;
}
